# 足球場邊記錄器 v2.42

## 紀錄視窗修正
- 修正紀錄彈窗只顯示少數事件的問題。
- 原因是共用 `.log` CSS 仍有 `max-height:150px`，即使外層彈窗變大，內層事件清單仍被限制在 150px。
- 紀錄彈窗內的 `.log` 改成不限制高度，實際高度由彈窗中間的可捲動區控制。
- 彈窗高度改為盡量使用 iPhone Safari 的 visualViewport，可顯示更多事件。
- 標題列與底部關閉區縮短，把更多空間留給事件。
- 每筆事件新增「第1筆、第2筆…」序號。
- 紀錄超過一頁時仍可在彈窗中順暢上下捲動。
