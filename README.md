# 足球場邊記錄器 v2.78

本版更新：

1. 自訂規定時間超過 45 分鐘時，會立即自動調整為 45，並顯示「最多 45 分鐘，已自動調整為 45」。
2. 自訂時間輸入框補強數字鍵盤設定：`inputmode=numeric`、`pattern=[0-9]*`、`enterkeyhint=done`。
3. 貼上或輸入非數字內容時會自動移除，只保留數字。
4. 輸入有效值時，提示會顯示「自訂 N 分鐘」；空白時恢復一般提示。
5. 自訂值仍限制為 1～45 分鐘整數。
